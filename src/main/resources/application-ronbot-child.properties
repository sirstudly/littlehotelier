# Loaded by each ronbot property child context (with crh/hsh/rmb/lsh).
# Keep pools tiny: four children share one MySQL server with the processors.
db.poolsize.min=1
db.poolsize.max=2
shareddb.poolsize.min=0
shareddb.poolsize.max=1
db.connection.timeout.ms=15000
chrome.for.testing.enabled=false
